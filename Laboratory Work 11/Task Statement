Task statement:
A polynomial with integer coefficients can be represented as a list.
Set the polynomial of X as a simply connected list.
The list item contains an exponent of X and a non-zero coefficient for this degree
(there should not be elements with the same degrees in the list).
Create a program that includes, in addition to the functions specified in the task, functions for creating and displaying a list on the screen.
The list or lists should be displayed on the screen before processing and after.
Write a function that forms a polynomial (list) of two polynomials L1, L2 according to the following rule:
the new polynomial includes those elements from L1 whose degrees are not included in L2. For example:
L1 = - 5x^2 + x^3 + x-3x^5
L2 = 2x^2-4x^5 + 7, then
L = x^3 + x


Постановка задачи:
Многочлен с целыми коэффициентами можно представить в виде списка.
Задать многочлен от Х односвязным списком.
Элемент списка содержит показатель степени Х и ненулевой коэффициент при этой степени
(в списке не должно быть элементов с одинаковыми степенями).
Составить программу, включающую помимо указанных в задании функций, функции создания и вывода списка на экран.
Список или списки должны отображаться на экране до обработки и после.
Написать функцию, формирующую многочлен (список) из двух многочленов L1, L2 по следующему правилу:
в новый многочлен включаются те элементы из L1, степени которых не входят в L2. Например:
L1 = -5x^2 + x^3 + x - 3x^5
L2 = 2x^2 - 4x^5 + 7, тогда
L = x^3 + x
